/**
 * HTTP-Schicht ([docs/01]). Der reine, testbare Router (`router.ts`) und die dünne
 * `node:http`-Schale (`server.ts`), die ihn bedient.
 */

export * from "./router.ts";
export * from "./server.ts";
